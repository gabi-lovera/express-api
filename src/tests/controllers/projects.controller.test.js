import project from '../mocks/projects.json' assert { type: 'json' }
import {
  getProject,
  getProjects,
  updateProject,
  createProject,
  deleteProject,
} from '../../app/controllers/projects.controller.js'

describe('first', () => {
  it('should first', () => {
    console.log('test1')
  })
  it('should first', () => {
    console.log('test2')
  })
})
